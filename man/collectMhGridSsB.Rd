% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-ensemble.R
\name{collectMhGridSsB}
\alias{collectMhGridSsB}
\title{Collect SSB time series for an M × steepness directory grid}
\usage{
collectMhGridSsB(root, M, h, scen = NULL)
}
\arguments{
\item{root}{Root path containing subfolders \code{M/h/}.}

\item{M}{Numeric vector of M multipliers (same length as \code{h} or use
\code{expand.grid} via \code{scen}).}

\item{h}{Numeric vector of steepness values.}

\item{scen}{Optional \code{data.frame} from \code{\link{mhScenarioGrid}}; if supplied,
\code{M} and \code{h} are ignored.}
}
\value{
A single \code{tibble} with columns \code{M}, \code{h}, \code{year},
\code{spawn_bio}, \code{spawn_bio_sd}, \code{model_id}.
}
\description{
Expects folders \code{file.path(root, M[i], h[i])} for each row in
\code{\link{mhScenarioGrid}(M, h)} (same layout as \code{\link{runSs3MhGrid}}).
}
