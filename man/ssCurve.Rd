% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve-ss-utils.R
\name{ssCurve}
\alias{ssCurve}
\title{Process-error curves across SS3 runs}
\usage{
ssCurve(
  x,
  col = "run",
  for_plots = FALSE,
  cache = TRUE,
  parallel = TRUE,
  workers = NULL,
  ...
)
}
\arguments{
\item{x}{Assessment parent directory, \code{ssRuns()} table, or single run directory.}

\item{col}{Run id column name (default \code{"run"}).}

\item{for_plots}{Include columns needed for PE diagnostic figures; may call
\code{FLRebuild::curveSS} when enrichment is needed.}

\item{cache}{Read cached \code{ss_output.rds} per run.}

\item{parallel, workers}{Parallel \code{curveSS} calls.}

\item{...}{Passed to \code{curveSS()} / \code{ssRead()}.}
}
\value{
Named list with combined \code{tseries}, \code{curve}, and \code{refpts}
data frames, or \code{NULL} if no runs succeed.
}
\description{
Runs \code{curveSS()} on every nested scenario under an assessment base and
row-binds \code{tseries}, \code{curve}, and \code{refpts}. Uses cached
\code{ss_output.rds} when available (run \code{SS_outputs()} first).
}
