% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-lencomp.R
\name{ssLenInd}
\alias{ssLenInd}
\title{Length-based indicators from SS3 length compositions}
\usage{
ssLenInd(
  x,
  indicators = c("lbar", "lmean", "l95", "l25", "lc50", "lmax5"),
  cache = TRUE,
  col = "run",
  parallel = TRUE,
  workers = NULL,
  ...
)
}
\arguments{
\item{x}{Assessment parent directory, \code{ssRuns()} table, SS3 run directory,
\code{lendbase} input accepted by \code{ssLenFlq()}, or an \code{FLQuants}
object.}

\item{indicators}{Character vector of \code{FLCore} indicator names (default:
\code{lbar}, \code{lmean}, \code{l95}, \code{l25}, \code{lc50}, \code{lmax5}).}

\item{cache}{Read cached \code{ss_output.rds} when building \code{FLQuants}.}

\item{col}{Run id column name when \code{x} spans multiple scenarios.}

\item{parallel, workers}{Parallel over runs when \code{x} is an assessment base.}

\item{...}{Extra arguments passed to indicators (\code{linf}, \code{k},
\code{lenwt}, etc.) and to \code{ssLenFlq()}.}
}
\value{
Data frame with \code{run} (if multiple scenarios), \code{fleet},
\code{year}, optional \code{unit}/\code{season}, \code{indicator}, and
\code{value}.
}
\description{
Converts length compositions to \code{FLQuants} (\code{\link{ssLenFlq}}) and
computes ICES-style length indicators using \code{FLCore} (\code{lmean},
\code{lbar}, \code{l95}, etc.).
}
