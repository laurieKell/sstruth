% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-workflow.R
\name{loadSstruth}
\alias{loadSstruth}
\title{Load the \strong{sstruth} package (development tree or installed)}
\usage{
loadSstruth(
  sstruthRoot = Sys.getenv("SSTRUTH_ROOT", unset = ""),
  quiet = TRUE,
  requireFn = NULL
)
}
\arguments{
\item{sstruthRoot}{Path to the \strong{sstruth} source directory. Default:
\code{Sys.getenv("SSTRUTH_ROOT", unset = "")}.}

\item{quiet}{Passed to \code{pkgload::load_all()} when used.}

\item{requireFn}{Optional length-one character: a name that must exist as a
function after load (e.g. \code{"runRetros"}). If missing, stops with a clear error.}
}
\value{
Invisibly: \code{character(1)} — the root path used, or \code{""} when
the installed package was loaded from the library.
}
\description{
If \code{SSTRUTH_ROOT} points at a package source tree (contains
\code{DESCRIPTION}), uses \code{pkgload::load_all()} when available; otherwise
\code{library(sstruth)}. Use this in R Markdown and case-study scripts so paths
stay configurable while generic workflow code lives in the package.
}
