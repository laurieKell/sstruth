% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudodataPipeline.R, R/ss-scenarios.R
\name{ssScenarios}
\alias{ssScenarios}
\title{Expand stock/scenario definitions into task list}
\usage{
ssScenarios(
  base = NULL,
  studyId = NULL,
  appendixDir = NULL,
  register = NULL,
  discover = TRUE,
  listRuns = TRUE,
  ssOutput = NULL,
  flStock = NULL,
  artifacts = FALSE,
  listOnly = FALSE,
  outDir = NULL,
  texOut = NULL,
  refresh = FALSE,
  parallel = TRUE,
  workers = NULL,
  caption = NULL,
  label = "tab:scenario-catalog"
)

ssScenarios(
  base = NULL,
  studyId = NULL,
  appendixDir = NULL,
  register = NULL,
  discover = TRUE,
  listRuns = TRUE,
  ssOutput = NULL,
  flStock = NULL,
  artifacts = FALSE,
  listOnly = FALSE,
  outDir = NULL,
  texOut = NULL,
  refresh = FALSE,
  parallel = TRUE,
  workers = NULL,
  caption = NULL,
  label = "tab:scenario-catalog"
)
}
\arguments{
\item{base}{Assessment parent (or from study spec / \code{DIAG_SS_BASE}).}

\item{studyId, appendixDir}{Study bundle location.}

\item{register}{Scenario register list or \code{study_spec()$scenarios}.}

\item{discover}{Scan \code{base} for all \code{Report.sso} runs.}

\item{listRuns}{Step i — discover runs and write catalog (+ LaTeX).}

\item{ssOutput}{Step ii — \code{SS_outputs()} into each run folder.}

\item{flStock}{Step iii — \code{readFLSss3()} into each run folder.}

\item{artifacts}{If \code{TRUE}, also run \code{ssExportRuns()} (CPUE/lencomp inputs/fits).}

\item{listOnly}{Step i only (\code{listRuns} without ii/iii).}

\item{outDir}{Catalog CSV/RDS directory.}

\item{texOut}{LaTeX table path.}

\item{refresh, parallel, workers}{Passed to \code{SS_outputs()} / \code{readFLSss3()}.}

\item{caption, label}{Table caption and label.}

\item{stocks}{List of stock specifications.}
}
\value{
List of task entries.
}
\description{
\enumerate{
\item List nested scenarios (\code{ssRuns()} + \code{ssCatalog()}).
\item Cache \code{SS_output} in each run folder (\code{SS_outputs()}).
\item Cache \code{FLStock} in each run folder (\code{readFLSss3()}).
}
}
