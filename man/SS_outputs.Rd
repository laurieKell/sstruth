% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-read.R
\name{SS_outputs}
\alias{SS_outputs}
\title{Read and cache \code{SS_output} for all runs under a directory}
\usage{
SS_outputs(
  x,
  covar = FALSE,
  forecast = FALSE,
  refresh = FALSE,
  parallel = TRUE,
  workers = NULL
)
}
\arguments{
\item{x}{Assessment parent directory to scan, or a \code{ssRuns()} table with
columns \code{id} and \code{path}.}

\item{covar, forecast}{Passed to \code{r4ss::SS_output()}.}

\item{refresh}{Re-read \code{Report.sso} even when cached.}

\item{parallel, workers}{Parallel collection.}
}
\value{
Status data frame per run (\code{id}, \code{path}, \code{cache}, \code{ok}, ...).
}
\description{
Like \code{r4ss::SS_output()}, but discovers every nested \code{Report.sso}
(via \code{ssRuns()}) and writes \code{ss_output.rds} into each run folder.
}
