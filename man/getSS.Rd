% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-read.R
\name{getSS}
\alias{getSS}
\title{Read cached \code{SS_output} and bind one slot as a data frame}
\usage{
getSS(
  x,
  slot,
  col = "scenario",
  cache = TRUE,
  covar = FALSE,
  forecast = FALSE,
  refresh = FALSE,
  parallel = TRUE,
  workers = NULL
)
}
\arguments{
\item{x}{Assessment parent directory, single run directory, \code{ssRuns()} table,
or named list of \code{SS_output} objects.}

\item{slot}{Slot name (case-insensitive), e.g. \code{"kobe"}, \code{"timeseries"}.}

\item{col}{Scenario id column name.}

\item{cache}{If \code{TRUE}, require cached output (use after \code{SS_outputs()}).}
}
\description{
Loads \code{ss_output.rds} from run folder(s) (run \code{SS_outputs()} first),
extracts \code{slot} via \code{ssSlot()}, and row-binds into one data frame with
a scenario column.
}
