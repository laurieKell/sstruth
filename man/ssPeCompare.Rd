% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-PE.R
\name{ssPeCompare}
\alias{ssPeCompare}
\title{Compare SS production-function signal against PT fits}
\usage{
ssPeCompare(
  tseries,
  eqlYield,
  shapePt,
  bmsySsb = NULL,
  makePlots = TRUE,
  pointCol = "grey40",
  eqCol = "black",
  ptColB = "blue",
  ptColSsb = "red"
)
}
\arguments{
\item{tseries}{Data frame containing at least \code{year}, \code{biomass}, \code{ssb},
\code{yield}, and \code{pf}. If \code{pe} is missing, reference process error is
recomputed from \code{pf}.}

\item{eqlYield}{Data frame containing SS equilibrium curve columns \code{ssb} and
\code{yield}.}

\item{shapePt}{Numeric scalar in \verb{(0, 1)}, interpreted as \code{Bmsy/K} for the
PT mapping.}

\item{bmsySsb}{Optional positive scalar. If provided, constrains the SSB PT
fit via \code{K = bmsySsb / shapePt}.}

\item{makePlots}{Logical; if \code{TRUE}, produces three ggplot panels (biomass
SP, SSB SP, and process error).}

\item{pointCol}{Point and path color for observed clouds.}

\item{eqCol}{Reserved equilibrium color argument for compatibility.}

\item{ptColB}{PT-curve color for biomass-space fits.}

\item{ptColSsb}{PT-curve color for SSB-space fits.}
}
\value{
An (invisible) list with elements:
\describe{
\item{pars}{Parameter summary table for biomass and SSB PT fits.}
\item{pe}{Year-wise process-error series (reference and PT-based).}
\item{diagnostics}{Curve and PE similarity metrics including
\code{curve_cor}, \code{curve_rel_rmse}, \code{pe_cor_ssb}, and a
\code{likely_misspecified} flag.}
\item{biomass}{Fit object, observed points, and fitted curve in biomass space.}
\item{ssb}{Fit object, observed points, and fitted curve in SSB space.}
}
}
\description{
Fits Pella-Tomlinson (PT) production curves in biomass and SSB space, compares
them to SS-derived production-function behavior, computes process-error series,
and optionally generates ggplot diagnostics.
}
