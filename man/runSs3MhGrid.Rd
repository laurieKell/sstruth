% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-mh-grid.R
\name{runSs3MhGrid}
\alias{runSs3MhGrid}
\title{Run an M × steepness grid of Stock Synthesis models}
\usage{
runSs3MhGrid(
  root,
  filesSubdir = "files",
  ctlfile,
  M,
  h,
  ssExe = "ss3",
  runSs3 = TRUE,
  overwrite = TRUE,
  parallel = FALSE,
  workers = NULL,
  natmRegex = "#\\\\s*natm",
  showInConsole = FALSE
)
}
\arguments{
\item{root}{Path containing the \code{filesSubdir} template tree.}

\item{filesSubdir}{Subdirectory of \code{root} with inputs (default \code{"files"}).}

\item{ctlfile}{Control file name.}

\item{M}{Numeric vector of M multipliers.}

\item{h}{Numeric vector of steepness values.}

\item{ssExe}{Passed to \code{r4ss::run}.}

\item{runSs3}{If \code{FALSE}, only set up directories and control files.}

\item{overwrite}{Passed to \code{\link{setupSs3MhScenario}}.}

\item{parallel}{If \code{TRUE}, run scenarios in parallel (one worker per scenario
up to \code{workers}).}

\item{workers}{Max parallel workers; \code{NULL} uses \code{parallelly::availableCores(omit = 1)}.}

\item{natmRegex}{Passed to \code{\link{patchCtlNatmMultiplier}}.}

\item{showInConsole}{Passed to \code{r4ss::run}.}
}
\value{
Invisibly, a \code{data.frame} with columns \code{M}, \code{h}, \code{dir},
and \code{ok} (logical or \code{NA} if SS not run).
}
\description{
For each combination in \code{\link{mhScenarioGrid}(M, h)}, copies
\code{file.path(root, filesSubdir)} to \code{file.path(root, M, h)}, patches the
control file, and optionally runs SS3. Parallel runs use \code{future.apply} when
\code{parallel = TRUE} and \code{future} is available.
}
