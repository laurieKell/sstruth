% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss-mh-grid.R
\name{setupSs3MhScenario}
\alias{setupSs3MhScenario}
\title{Copy a base SS3 folder and patch M (multiplier) and steepness for one scenario}
\usage{
setupSs3MhScenario(
  baseFilesDir,
  destDir,
  ctlfile,
  M_multiplier,
  steepness,
  overwrite = TRUE,
  natmRegex = "#\\\\s*natm"
)
}
\arguments{
\item{baseFilesDir}{Directory to copy from (see \code{\link{ssCopyTree}}).}

\item{destDir}{Destination scenario directory (created or overwritten per
\code{overwrite}).}

\item{ctlfile}{Name of the control file inside the scenario (e.g. \code{"control.ss"}).}

\item{M_multiplier}{Natural mortality multiplier applied to \code{# natm} lines.}

\item{steepness}{Steepness (\code{BH_steep}) passed to \code{\link{patchCtlBhSteepness}}.}

\item{overwrite}{If \code{TRUE}, replace an existing \code{destDir}.}

\item{natmRegex}{Passed to \code{\link{patchCtlNatmMultiplier}}.}
}
\value{
Invisibly, normalized \code{destDir}.
}
\description{
Typical layout: \code{baseFilesDir} is \code{file.path(root, "files")} with a full
model tree; output goes to e.g. \code{file.path(root, M, h)}.
}
